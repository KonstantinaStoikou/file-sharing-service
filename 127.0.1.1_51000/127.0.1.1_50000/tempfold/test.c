/**
 * A main to make tests on new features
 *
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "include/circular_buffer.h"

int main(int argc, char const *argv[]) {
    // Circular_buffer *cb = initialize_circ_buf(5, sizeof(Cb_data));

    // Cb_data *data1 = malloc(sizeof(Cb_data));
    // strcpy(data1->pathname, "hello1");
    // data1->version = 1;
    // Cb_data *data2 = malloc(sizeof(Cb_data));
    // strcpy(data2->pathname, "hello2");
    // data2->version = 2;
    // Cb_data *data3 = malloc(sizeof(Cb_data));
    // strcpy(data3->pathname, "hello3");
    // data3->version = 3;

    // push_back_circ_buf(cb, data1);
    // push_back_circ_buf(cb, data2);
    // push_back_circ_buf(cb, data3);
    // free(data1);
    // free(data2);
    // free(data3);
    // print_circ_buf(cb);
    // delete_circ_buf(cb);

    return 0;
}
