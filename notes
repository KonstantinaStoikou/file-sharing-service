NOTES:

- pou ginetai to copy? se poion fakelo? (clientName/pathname -> pou vrisketai?)

TERMINAL COMMANDS:
check listening ports:  sudo ss -lntu
send message to server: echo "hello" | nc 127.0.0.1 8888
free port numbers:      49152 - 65535

./dropbox_client -d hello -p 50000 -w 10 -b 100 -sp 8888 -sip localhost

TO DO:
- see select() function
- before log_off message, stop threads and free memory
- get file
- generally break functions to more files
- make threads start reading from buffer
- add to thread arguments list the name of the local folder to store backups
- blocking vs reading when buffer is empty

READ_ME:
- request format (how request strings are given in sockets)