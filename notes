NOTES:

- LOG_OFF vs USER_OFF minima apo server
- to server ip pos dinetai san orisma? string int??
- port client san orisma kai port client pou vrisketai genika?

- maybe split handle server connection to smaller functions

TERMINAL COMMANDS:
check listening ports:  sudo ss -lntu
send message to server: echo "hello" | nc 127.0.0.1 8888
free port numbers:      49152 - 65535

./dropbox_client -d hello -p 50000 -w 10 -b 100 -sp 8888 -sip localhost

TO DO:
- client main thread (first paragraph)
    - getclients don't send same client
    - parse client list message and add to list struct